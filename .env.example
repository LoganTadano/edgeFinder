# Postgres credentials — pick whatever you want, these are just for local dev
POSTGRES_USER=logan
POSTGRES_PASSWORD=edgefinder123
POSTGRES_DB=edgefinder

# This is what your Python backend uses to connect
# IMPORTANT: hostname must be 'db' not 'localhost' (Docker service name)
DATABASE_URL=postgresql://logan:edgefinder123@db:5432/edgefinder

# Get this from balldontlie.io — free account, takes 2 minutes
BALLDONTLIE_API_KEY=your_key_here

# Leave this as-is for local development
VITE_API_URL=http://localhost:8000
